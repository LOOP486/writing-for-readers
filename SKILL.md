---
name: writing-for-readers
description: "写作/修改任何文档时使用——设计文档、方案、说明、报告、README、需求、邮件、提案、日报等。先分清文档给谁看：给用户本人或协作 AI 看的（内）可保留状态标注、待确认等提醒；给其他人看的（外）严格清除内部口吻。约束 AI 写作通病：泄露内部口吻、冗长不凝练、反复啰嗦、生造新词故作高深、把方法论框架（机制轴/闭环/自检/T1T2T3/验收尺子）当正文摆出来（一股 AI 味/填表味的主因）。触发词：写文档、写方案、写说明、写报告、润色、精简、这段太啰嗦、像 AI 写的、一股 AI 味。"
---

# 面向读者写作

写文档前先问一件事：**这份东西给谁看，他看完要能干什么？** AI 写文档的通病都源于跳过了这一步——只顾把想到的都写下来，不顾读者的处境。这个 skill 用一条主线和四条硬规则约束写作。

## 主线：先定读者，再动笔

动笔前先在心里明确（不必写进文档）：

- **读者是谁**——外部客户 / 上级 / 平级同事 / 完全不了解背景的人。默认假设是**外人**：不知道你的工作过程、不关心你怎么想的、只想拿到有用的结论。
- **他看完要能干什么**——做决策？照着执行？了解现状？目标决定内容取舍。
- **他已经知道什么**——不要重复他已知的；也不要假设他知道他不知道的。

写每一段前自问：**这句话对读者有用吗？** 没用就删。

## 先分清"内"和"外"

- **内**：给**用户本人**看的，或给**协作的 AI**看的（工作稿、草稿、给下个会话/下个 agent 接手的文件）。
- **外**：给**用户以外的其他人**看的（同事、上级、客户、评审、任何外部读者）。

**默认所有文档都是"外"**，按下面四条硬规则严格处理。

**"内"的例外**：当文档是给用户自己看、或给协作 AI 看时，**允许并鼓励**保留内部断言——状态标注、待办清单、🔶❓这类审阅脚手架、"这里待定/存疑/待老师确认"的自指。这些在内部起**提醒和标注**作用，是有用的，不要删。即便是内部稿，规则 2/3/4（凝练、不反复、不造词）仍然适用，只有规则 1（不泄露内部口吻）放松。

判断依据：**这份文档最终会不会到用户以外的人手里？** 会→按外处理；只在用户和 AI 之间流转→按内处理。不确定时按外处理，或直接问用户一句"这份给谁看"。

---

## 四条硬规则

### 规则 1：不泄露内部口吻（对外文档最致命）

外人一眼认出"这是 AI 写的/这是没写完的草稿"，就不愿意读了。删掉一切**创作过程的痕迹**和**元评论**：

**要删的表述（对外文档中）：**

- 状态标注：`本文档为暂存版`、`此节待审核`、`以下内容待确认`、`初稿`、`v0.1 草稿`
- AI 自指与判断口吻：`以下是我的判断`、`我认为`、`需要说明的是`、`值得注意的是`、`综上所述`（讲结论就好，别宣布"我要讲结论了"）
- 对自己工作的评价：`这是一个不错的方案`、`本方案较为完善`
- 占位与未完成痕迹：`（此处待补充）`、`TODO`、`xxx`、`具体数值待定`留在正文里
- 审阅脚手架：🔶❓ 这类给评审看的记号、"存疑""待老师确认"的旁注

**正确做法：** 直接陈述内容本身。文档的成熟度、待确认事项，放在**单独的沟通渠道**（邮件正文、口头、单独的内部清单文件），不要混进给外人看的正文。如果确有未定项必须让外部读者知道，用中性、确定的表述：`该数值将在 X 阶段确定`，而不是`这个我还没想好`。

### 规则 2：凝练——一句能说清就不写两句

- 删修饰性废话：`非常`、`极其`、`相当`、`一定程度上`、`在很大程度上`、`总的来说`。
- 长句拆短，或砍掉从句。读者读一遍就懂，就是好句子。
- 能用词就不用短语，能用短语就不用句子。
- 每写完一段，回头删掉 20%——通常删了更清楚。

### 规则 3：不反复——一个点只讲一次

- 同一个信息只在**最该出现的地方**讲一次。开头讲了结论，正文别再原样重复。
- 不要"预告—正文—总结"三遍讲同一件事，除非文档很长确实需要摘要。
- 检查：如果两段话删掉一段不损失信息，就删。

### 规则 4：不造词、不故作高深

- 用**行业通用词**和**读者熟悉的词**。不要生造概念（如把"逐个检查"包装成"单元级校验闭环"）。
- 不为了显得专业而堆术语、套大词。简单的事就简单说。
- 造新词的唯一理由是：这个概念反复出现且没有现成词——此时**第一次出现就用一句话定义清楚**，之后才能简称。
- 警惕这类词：`赋能`、`抓手`、`闭环`、`心智`、`颗粒度`、`对齐`（滥用时）、以及任何你自己造的、读者第一次见的组合词。

### 规则 5：方法论术语是"内部脚手架"，对外别当正文（最容易忽略）

写方案/设计文档时，你脑子里会先过一套**检查框架**——五问检验法、机制轴、闭环、三层压力、验收尺子、T1/T2/T3、自检清单……**这些是你设计时用的工具，不是给读者的内容。** 团队内部、知识库里用这套话没问题，但一旦把它们当章节标题和正文原样搬进交付件，内行一眼就认出"这是拿模板套出来的"，比啰嗦更致命——它让整篇透着"AI/新人填表"的味儿。

**注意：术语来自团队内部或知识库，不等于可以对外堆。** 内部沟通用它们是共识速记；对外它们是"我在向你展示我走了流程"，恰恰是读者最反感的自我表演。别人也用 AI、也套框架，不是你照做的理由。

**要清的脚手架（对外文档中）：**

- 框架章节名：`独有机制轴`、`高光情境`、`物资获取闭环`、`三层压力`、`空间组织 T1/T2/T3`、`五问自检`、`独有性校验`、`验收尺子`——把它作为标题/字段名摆出来就是暴露。
- 自我打分区：把"我按 X 法自查过了"做成清单附在文末。**人不会在方案里附一份自己给自己的评分表。**
- 过度工整：每节都字段对齐、每条都破折号解释、结构齐到不像人手写——齐到失真就假。

**正确做法：** 把框架**用掉、别写出来**。检查过"资源是不是主语"→直接写清玩家来干嘛、拿什么走，不写"物资获取闭环"这个词。想清楚了三层压力→直接讲这关怎么让人紧张，不列"基础/特色/压力结果"三行表。**留下框架逼出来的内容，删掉框架本身。** 读起来应该像一个懂行的人在讲这个东西，而不是像一张填好的表。

---

## 写完后的自检清单

改完/写完，逐条过一遍：

1. **读者**：一个不了解背景的目标读者，读完能拿到他需要的东西吗？
2. **内部口吻**：有没有状态标注、AI 自指、"我认为/需要说明的是"、待审核记号？（对外文档必须为零）
3. **凝练**：有没有能删的修饰词、能拆的长句、能砍的废话？
4. **反复**：有没有同一个点讲了两遍以上？
5. **造词**：有没有生造的、读者第一次见就看不懂的词？有没有为显高深堆的术语？
6. **方法论脚手架**：章节名/字段里有没有把设计框架（机制轴、闭环、三层压力、五问自检、T1/T2/T3、验收尺子）当正文摆出来？有没有自我打分清单？读起来像懂行的人在讲，还是像填好的表？

## 常见反例 → 改法

| 反例（AI 常写） | 改法 |
|---|---|
| `本文档为初稿，部分内容待确认，仅供参考` | 删掉整句（对外别放）；成熟度另行沟通 |
| `需要特别说明的是，该方案在很大程度上能够有效提升整体效率` | `该方案提升效率` |
| `综上所述，基于以上分析，我认为应当采用方案 A` | `采用方案 A。理由：……` |
| `我们需要构建一个用户价值感知闭环` | `让用户用完就知道好在哪` |
| 开头、正文、结尾各讲一遍同样的三点 | 只在正文讲一次，开头最多一句话概括 |
| `## 独有机制轴：垂直逐层搜刮 + 翻找压力（噪音→尸潮）` | `这是一栋封死的居民楼，从下往上一层层搜居民囤的货，翻得越狠动静越大，引来尸潮。` |
| 文末附 `## 五问自检：1. 一句话立住？✅ …` | 整节删掉——自查是你的事，不摆给读者看 |
| `三层压力`／`空间组织（T1/T2/T3）`／`物资获取闭环` 作章节标题 | 用框架想清楚，但写出来就用大白话讲这关怎么玩、怎么紧张、怎么走 |
